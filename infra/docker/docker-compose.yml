version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: poc_lp_db
    restart: unless-stopped
    ports:
      - "${POSTGRES_PORT:-5434}:5432"     # expõe 5434 no host (não conflita com outros)
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - ../../db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

# opcional: pgAdmin (UI web) — habilite quando quiser
#  pgadmin:
#    image: dpage/pgadmin4:8
#    container_name: poc_lp_pgadmin
#    restart: unless-stopped
#    ports:
#      - "8081:80"
#    environment:
#      PGADMIN_DEFAULT_EMAIL: admin@local
#      PGADMIN_DEFAULT_PASSWORD: admin
#    depends_on:
#      - postgres
